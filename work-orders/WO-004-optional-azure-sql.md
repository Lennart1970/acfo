# WO-004 — Optional Azure SQL knowledge replica

**Audience:** Microsoft administrator  
**When:** Only if PostgREST-as-a-tool is not enough

## Ask

Copy `transaction_lines_incremental` to Azure SQL and add those tables as Copilot Studio *knowledge*. Supabase remains the MVP source of truth.

## Acceptance

- Azure SQL has the incremental lines
- Copilot knowledge is scoped to finance tables only
